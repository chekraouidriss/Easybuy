# Easybuy
The platform uses the Stripe API to process payments.Communication with customers is automated via an SMTP service configured to systematically send transactional emails. Security has been strengthened at multiple levels: CSRF protection, custom middleware to control access, two-factor authentication (2FA), and IP restrictions for sensitive interfaces.
